# Configuration Railway avec variables d'environnement officielles
# Ces variables sont automatiquement injectées par Railway

# Base de données MySQL
DATABASE=${MYSQL_DATABASE}
USER=${MYSQLUSER}
PASSWORD=${MYSQL_ROOT_PASSWORD}
HOST=${MYSQLHOST}
PORT_DATABASE=${MYSQLPORT}
DIALECT=mysql

# Serveur
PORT=${PORT:-3000}
NODE_ENV=production

# CORS (URL de ton frontend)
FRONTEND_URL=https://incuneoi.vercel.app

# JWT Secret
JWT_SECRET=incuneo_jwt_secret_2024_production

# URLs de connexion MySQL (pour référence)
MYSQL_PUBLIC_URL=${MYSQL_PUBLIC_URL}
MYSQL_URL=${MYSQL_URL} 